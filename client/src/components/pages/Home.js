import React, { Fragment } from 'react';
import Library from '../library/Library';

export const Home = () => {
  return (
    <Fragment>
      <Library />
    </Fragment>
  );
};

export default Home;
